# UI Flow - ReceiptApp

Dưới đây là phân tích giao diện hiện tại và đề xuất luồng UI cho ứng dụng `ReceiptApp`.

## Tổng quan dự án
- Entry point: `MainActivity` (Jetpack Compose)
- Hiện tại chỉ có 1 màn hình hiển thị text `Hello Android!`.
- Thư mục theme có `Color.kt`, `Type.kt`, `Theme.kt`.

## Luồng UI hiện tại
```
[Launcher]
  |
  v
[MainActivity]
  |
  v
[Home: Greeting("Android")]
```
- Màn hình `Home` hiện tại chỉ hiển thị một `Text` đơn giản.

## Đề xuất luồng UI (mở rộng)
Mục tiêu: chuyển app từ template thành app quản lý hóa đơn/biên lai với các màn chính.

```
[Launcher / Splash]
  |
  v
[Onboarding?]
  |
  v
[Login / Guest]
  |
  v
[Home: List Receipts]
   |\
   | \-> [Receipt Details]
   | \-> [Add Receipt] -> [Scan / Upload Photo] / [Manual Entry]
   | \-> [Reports / Charts]
   \-> [Settings]
```

## Mô tả màn hình chính và hành vi
- **Splash / Launcher**: Hiển thị logo, kiểm tra auth/local DB/remote sync.
- **Onboarding** (tùy chọn): Giới thiệu tính năng như quét hoá đơn, xuất báo cáo.
- **Auth (Login/Guest)**: Hỗ trợ đăng nhập bằng email, Google/Firebase, hoặc chế độ khách.
- **Home - List Receipts**: Danh sách hóa đơn, lọc theo ngày, tìm kiếm, sort, tổng tiền.
- **Receipt Details**: Hiển thị hình ảnh hóa đơn, trường dữ liệu (vendor, date, amount, category), nút chỉnh sửa/xóa.
- **Add Receipt**: Chọn Scan (OCR), Upload, hoặc nhập tay.
- **Reports**: Biểu đồ tổng chi theo tháng, theo category, export CSV/PDF.
- **Settings**: Cài đặt theme, language, backup/restore, export, privacy.

## Kiến trúc đề xuất
- UI: Jetpack Compose + Navigation Compose (`androidx.navigation:navigation-compose`)
- State: ViewModel (`androidx.lifecycle:viewmodel-ktx`) + StateFlow
- Data layer: Room cho offline storage, Repository pattern
- Network: Retrofit + OkHttp (nếu sync cloud)
- DI: Hilt hoặc Koin
- OCR: ML Kit Text Recognition (on-device) hoặc Tesseract cho tuỳ chọn

## Cải tiến UX/Accessibility/Performance (chi tiết)
- **UX**: thêm Onboarding, Search, Filters, Quick Add (fab), clear empty states.
- **Accessibility**: dùng `contentDescription` cho ảnh, font scale, contrast đủ, hỗ trợ TalkBack.
- **Localization**: đưa mọi string vào `strings.xml` để dễ translate.
- **Dark Mode**: hiện đã có Material3 theme; đảm bảo colors tương thích và test.
- **Performance**: tối ưu image loading (coil), lazy lists (`LazyColumn`), paginate nếu nhiều item.
- **Testing**: Unit tests cho ViewModel, UI tests cho compose (Espresso + Compose Test).
- **Security & Privacy**: mã hoá dữ liệu nhạy cảm (Envelope / SQLCipher nếu cần), clear cache khi sign out.
- **CI/CD**: cấu hình GitHub Actions để build, lint, chạy tests và tạo bản phát hành.

## Quick wins (những bước nhỏ có thể làm ngay)
- Thay `Greeting` bằng một `HomeScreen` chứa `LazyColumn` và sample items.
- Thêm `Navigation` và một `ReceiptDetailScreen` rỗng.
- Đưa mọi màu/sizes vào `Theme` để dễ tuỳ biến.
- Thêm `strings.xml` và move hardcoded `"Hello Android!"` vào resource.

## Gợi ý file/tương lai PR
- `app/src/main/java/.../ui/home/HomeScreen.kt` - màn danh sách
- `app/src/main/java/.../ui/detail/ReceiptDetailScreen.kt`
- `app/src/main/java/.../navigation/NavGraph.kt`
- `app/src/main/java/.../data/ReceiptRepository.kt`
- `app/src/main/java/.../data/local/ReceiptDao.kt` (Room)

## Kết luận ngắn gọn
Hiện tại app chỉ là template Compose. Có nhiều hướng mở rộng: chuyển thành app quản lý hóa đơn với scan/OCR, lưu offline, báo cáo và xuất dữ liệu. Tôi đã vẽ luồng UI gợi ý trong file này và liệt kê cải tiến ưu tiên.

---

Nếu bạn muốn, tôi có thể tiếp tục bằng cách:
- Tạo `HomeScreen` và `NavGraph` mẫu (PR với code).
- Thêm Room + Repository skeleton.
- Tích hợp ML Kit OCR demo.

Cho tôi biết bạn muốn bước tiếp theo nào. 

---

## ASCII Wireframes (Full app)

Dưới đây là bản vẽ lại toàn bộ luồng và các màn bằng kỹ thuật ASCII wireframe giống ví dụ bạn cung cấp. Bạn có thể copy-paste trực tiếp vào Markdown, nó sẽ giữ layout monospace.

### Navigation flow (ASCII overview)
```
[Launcher]
  |
  v
[Splash] -> [Onboarding?] -> [Auth(Login/Guest)] -> [Home]
                      |                 |  \-> [Settings]
                      |                 |  \-> [Reports]
                      |                 \-> [Add Receipt]
                      \-> [Home (guest)]
```

### Splash
```
 ┌────────────────────────────────────────────────────────┐
 │                        SPLASH                           │
 │                                                        │
 │                      [  LOGO  ]                         │
 │                      AppName / Tagline                  │
 │                      (progress / Lottie)                │
 │                                                        │
 └────────────────────────────────────────────────────────┘
```

### Onboarding (single slide example)
```
 ┌────────────────────────────────────────────────────────┐
 │  [Image/Illustration]    Title: "Quickly scan receipts"  │
 │                                                       │
 │  Short description text explaining the feature.       │
 │                                                       │
 │  [ Prev ]   • • •   [ Next ]        [Skip | Get Started]
 └────────────────────────────────────────────────────────┘
```

### Auth / Profile (Sign In) - compact card (your style)
```
 ┌────────────────────────────────────────────────────────┐
 │ Profile 0% complete                                     │
 │ ░░░░░░░░░░ 0/3                                         │
 │ Complete: None                                         │
 └────────────────────────────────────────────────────────┘
                                           
 ┌────────────────────────────────────────────────────────┐
 │  [👤 Default Avatar]   │   Sign In                     │
 │  (No change btn)      │   (Click to sign in)          │
 │                       │                              │
 │  (Row layout: Avatar left, Sign In text right)        │
 └────────────────────────────────────────────────────────┘
```

### Home - Receipts List (compact)
```
 ┌────────────────────────────────────────────────────────┐
 │ TopBar: "Receipts"  [Search] [Filter] [⋮]             │
 └────────────────────────────────────────────────────────┘
 ┌────────────────────────────────────────────────────────┐
 │ [Chips: All] [This month] [Categories v]               │
 └────────────────────────────────────────────────────────┘
 ┌────────────────────────────────────────────────────────┐
 │ • ReceiptItem                                           │
 │   [thumb] Vendor name          2025-11-01    ₫ 1,200,000 │
 │   Category tag                                           │
 │ ------------------------------------------------------- │
 │ • ReceiptItem                                           │
 │   [thumb] Vendor name          2025-10-30    ₫ 450,000   │
 │ ------------------------------------------------------- │
 │ (LazyColumn scroll)                                      │
 └────────────────────────────────────────────────────────┘
                 [ + ] (FAB)
 [BottomNav] Home | Reports | Settings
```

### Receipt Details
```
 ┌────────────────────────────────────────────────────────┐
 │ < Back   Receipt #12345         [⋮]                    │
 └────────────────────────────────────────────────────────┘
 ┌────────────────────────────────────────────────────────┐
 │ [ Large photo of receipt - tap to zoom / swipe ]       │
 └────────────────────────────────────────────────────────┘
 ┌────────────────────────────────────────────────────────┐
 │ Vendor: ACME Store       Date: 2025-11-01    Total: ₫1,200,000
 └────────────────────────────────────────────────────────┘
 ┌────────────────────────────────────────────────────────┐
 │ Category: Groceries                                       │
 │ Payment: Card - Visa ****1234                             │
 │ Notes: Bought for family                                  │
 └────────────────────────────────────────────────────────┘
 Actions: [ Edit ] [ Delete ] [ Export PDF ] [ Share ]
 (Optional OCR panel: recognized lines with copy buttons)
```

### Add Receipt - Choose method
```
 ┌────────────────────────────────────────────────────────┐
 │ Add Receipt                                            │
 └────────────────────────────────────────────────────────┘
 ┌────────────────────────────────────────────────────────┐
 │ [ Scan (Camera) ]   [ Upload (Gallery) ]   [ Manual ]  │
 └────────────────────────────────────────────────────────┘
 
 Scan flow:
 ┌────────────────────────────────────────────────────────┐
 │ Camera preview -> [Capture] -> [Crop/Adjust] -> OCR    │
 │ -> Pre-filled form (Vendor, Date, Amount, Category)    │
 │ -> [ Save ]                                           │
 └────────────────────────────────────────────────────────┘
```

### Reports
```
 ┌────────────────────────────────────────────────────────┐
 │ Reports: [Date range selector]   [Export CSV/PDF]      │
 └────────────────────────────────────────────────────────┘
 ┌────────────────────────────────────────────────────────┐
 │ [ Card: Total spend ]  [ Card: Avg per month ]         │
 └────────────────────────────────────────────────────────┘
 ┌────────────────────────────────────────────────────────┐
 │ [ Chart area: Bar / Line / Pie ]                       │
 └────────────────────────────────────────────────────────┘
```

### Settings
```
 ┌────────────────────────────────────────────────────────┐
 │ Settings                                               │
 └────────────────────────────────────────────────────────┘
 ┌────────────────────────────────────────────────────────┐
 │ Account: [Profile]   [Logout]                          │
 │ Theme: ( ) Light   ( ) Dark   ( ) System               │
 │ Backup: [Backup now]  [Restore]   [Cloud connect]      │
 │ Security: [Enable PIN]  [Biometric]                    │
 │ About: App version, Privacy, Terms                     │
 └────────────────────────────────────────────────────────┘
```

---

Nếu bạn muốn, tôi có thể:
- Thay thế hoàn toàn các biểu đồ mermaid bằng các block ASCII tương tự (tạo bản sao của từng màn). 
- Hoặc chỉ chuyển một số màn chính (ví dụ `Home`, `Add Receipt`, `Receipt Details`).

Bạn muốn tôi chuyển toàn bộ mermaid sang ASCII không? (gõ `Toàn bộ` hoặc `Một vài` + danh sách màn)