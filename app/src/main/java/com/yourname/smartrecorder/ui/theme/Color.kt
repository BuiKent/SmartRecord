package com.yourname.smartrecorder.ui.theme

import androidx.compose.ui.graphics.Color

// Material3 Primary Color - Orange theme
val Primary = Color(0xFFFF6B35) // Vibrant orange
val OnPrimary = Color(0xFFFFFFFF)
val PrimaryContainer = Color(0xFFFFE5DE) // Light orange container
val OnPrimaryContainer = Color(0xFF5C1F00)

// Surface colors - Brighter, cleaner whites
val Surface = Color(0xFFFFFFFF) // Pure white instead of off-white
val OnSurface = Color(0xFF0F172A) // Darker for better contrast
val SurfaceVariant = Color(0xFFF1F5F9) // Light blue-gray, brighter
val OnSurfaceVariant = Color(0xFF475569) // Better contrast

// Background - Bright, fresh white with subtle blue tint
val Background = Color(0xFFFAFBFF) // Very light blue-white, fresh and bright
val OnBackground = Color(0xFF0F172A) // Darker for better readability

// Error
val Error = Color(0xFFBA1A1A)
val OnError = Color(0xFFFFFFFF)

